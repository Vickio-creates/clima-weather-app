# Image assets

The exact AI-generated UI images from earlier chats are not inside this ZIP unless they were uploaded manually.

Use this folder for:

- Figma screenshots
- AI-generated interface mockups
- Weather illustrations
- App screenshots for README/course paper

The recreated UI pages are included as HTML/CSS files, so they can be opened in the browser and screenshotted again for Figma or documentation.
